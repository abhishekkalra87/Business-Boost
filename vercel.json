{
  "buildCommand": "pnpm --filter @workspace/talentforge run build",
  "outputDirectory": "artifacts/talentforge/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
